import { DdddDirective } from './dddd.directive';

describe('DdddDirective', () => {
  it('should create an instance', () => {
    const directive = new DdddDirective();
    expect(directive).toBeTruthy();
  });
});
